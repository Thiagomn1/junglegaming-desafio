# Docker Compose para Desenvolvimento Local
# Sobe PostgreSQL, RabbitMQ e opcionalmente o frontend em modo dev
# Use: docker-compose -f docker-compose.dev.yml up -d

services:
  # Frontend em modo de desenvolvimento
  # web-dev:
  #   container_name: jungle-dev-web
  #   build:
  #     context: .
  #     dockerfile: ./apps/web/Dockerfile.dev
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./apps/web:/app/apps/web
  #     - ./packages:/app/packages
  #     - /app/node_modules
  #     - /app/apps/web/node_modules
  #   environment:
  #     - VITE_API_URL=http://localhost:3001
  #     - VITE_WS_URL=http://localhost:6000/notifications


  db:
    image: postgres:17.5-alpine3.21
    container_name: jungle-dev-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-challenge_db}
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    networks:
      - challenge-network-dev
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: jungle-dev-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER:-admin}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS:-admin}
    ports:
      - "5672:5672"   # AMQP
      - "15672:15672" # Management UI
    volumes:
      - rabbitmq_dev_data:/var/lib/rabbitmq
    networks:
      - challenge-network-dev
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_dev_data:
    name: jungle_dev_postgres_data
  rabbitmq_dev_data:
    name: jungle_dev_rabbitmq_data

networks:
  challenge-network-dev:
    driver: bridge
