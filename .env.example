# Este arquivo é para referência.
# As variáveis de ambiente estão configuradas individualmente em cada serviço.
#
# Copie este arquivo para .env se necessário para desenvolvimento local:
# cp .env.example .env

# Ambiente Global
NODE_ENV=development

# JWT Secret (compartilhado entre serviços)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# PostgreSQL (usado pelo Docker Compose)
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=challenge_db

# RabbitMQ (usado pelo Docker Compose)
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=admin
